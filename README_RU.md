# Инструкция по обновлению 25.12.2024

## Что было исправлено

Теперь при **повторном входе** пользователя в мини-приложение через Telegram бот, пользователь **сразу попадает в главное меню**, минуя стартовую страницу.

## Что изменилось

### 1. Сохранение локации в базу данных
Раньше выбранная локация сохранялась только локально в телефоне. Теперь она **сохраняется в Supabase** и синхронизируется с ботом.

### 2. Автоматическое восстановление локации
При втором и последующих входах приложение:
- Проверяет есть ли у пользователя сохраненная локация в БД
- Автоматически восстанавливает её
- Сразу показывает главное меню

### 3. Синхронизация с ботом
Telegram бот теперь знает последнюю выбранную локацию пользователя и открывает приложение сразу с нужной локацией.

## Измененные файлы

1. **lib/services/supabase_service.dart**
   - Добавлен метод `updateUserPreferredLocation()` для сохранения локации в БД

2. **lib/providers/location_provider.dart**
   - Добавлено автоматическое сохранение в БД при выборе локации
   - Добавлен метод `setUserId()` для привязки к пользователю

3. **lib/main.dart**
   - Добавлена установка userId в LocationProvider при инициализации

## Как проверить что всё работает

### Тест 1: Первый вход
1. Откройте бот в Telegram
2. Нажмите "📦 Открыть каталог"
3. Выберите локацию
4. ✅ Вы должны попасть в главное меню

### Тест 2: Второй вход (основной тест)
1. Закройте мини-приложение
2. Снова нажмите "📦 Открыть каталог" в боте
3. ✅ Вы должны **сразу попасть в главное меню** без выбора локации

### Тест 3: Смена локации
1. В главном меню смените локацию
2. Закройте приложение
3. Откройте снова через бота
4. ✅ Должна открыться **новая** выбранная локация

## Что делать если что-то не работает

### Если показывается стартовый экран при втором входе:

1. **Проверьте консоль браузера** (F12) - должны быть логи:
   ```
   ✅ Found preferredLocationId from DB: xxx
   ✅ Location matched from DB: xxx
   💾 Location automatically saved to DB
   ```

2. **Проверьте базу данных Supabase:**
   - Откройте таблицу `User`
   - Найдите своего пользователя по `telegram_user_id`
   - Проверьте что поле `preferredLocationId` заполнено

3. **Проверьте что бот запущен и подключен к той же БД**

## Технические детали

### Приоритеты восстановления локации:
1. `location_id` из hash параметров URL (от бота) - **Приоритет 0**
2. `preferredLocationId` из базы данных - **Приоритет 1**
3. Локация из локального хранилища - **Приоритет 2**
4. Первая доступная локация - **Приоритет 3**

### Логирование
Все ключевые действия логируются с эмодзи:
- 🔄 - Обновление данных
- ✅ - Успешная операция
- ⚠️ - Предупреждение
- ❌ - Ошибка
- 📍 - Операции с локацией
- 💾 - Сохранение в БД
- 🔍 - Поиск/проверка

### База данных
Используется таблица `User`, поле `preferredLocationId` (UUID).

## Разработчикам

Для сборки проекта:
```bash
flutter pub get
flutter build web
```

Для запуска локально:
```bash
flutter run -d chrome
```

Для проверки кода:
```bash
flutter analyze
```

## Контакты

При возникновении проблем проверьте:
1. Логи в консоли браузера (F12)
2. Логи Flutter (`flutter run`)
3. Логи Telegram бота
4. Данные в Supabase

---

**Дата обновления:** 25.12.2024
**Версия:** 3.1
**Статус:** ✅ Готово к тестированию
